<% if Setting.notified_events.include?('news_added') %>
  <% html = Nokogiri::HTML.fragment(render(:parent)) %>
  <% html.at_css("div:root.contextual") << watcher_tag(@project.news_subscription, User.current) %>
  <%= raw html %>
<% else %>
  <%= render(:parent) %>
<% end %>