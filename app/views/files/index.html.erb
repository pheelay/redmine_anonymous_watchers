<% if Setting.notified_events.include?('file_added') %>
  <% html = Nokogiri::HTML.fragment(render(:parent)) %>
  <% html.at_css("div:root.contextual") << watcher_tag(@project.files_subscription, User.current) %>
  <%= raw html %>
<% else %>
  <%= render(:parent) %>
<% end %>