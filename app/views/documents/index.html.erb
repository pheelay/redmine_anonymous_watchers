<% if Setting.notified_events.include?('document_added') %>
  <% html = Nokogiri::HTML.fragment(render(:parent)) %>
  <% html.at_css("div:root.contextual") << watcher_tag(@project.documents_subscription, User.current) %>
  <%= raw html %>
<% else %>
  <%= render(:parent) %>
<% end %>